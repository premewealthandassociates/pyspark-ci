name: Test action
on:
  workflow_dispatch:
jobs:
  container-job:
    runs-on: ubuntu-latest
    container:
      image: jupyter/pyspark-notebook
      options: --user root
    steps:
    - uses: actions/checkout@v3
    - name: ran in container
      run: |
        ls -la
        echo Hello